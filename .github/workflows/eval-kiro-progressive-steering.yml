name: Eval – Kiro Progressive Steering

on:
  pull_request:
    paths:
      - 'skill-forge/src/adapters/kiro*.ts'
      - 'skill-forge/templates/harness-adapters/kiro/**'
      - 'skill-forge/src/schemas.ts'
      - 'skill-forge/src/validate.ts'
      - 'skill-forge/src/wizard.ts'
      - 'skill-forge/src/eval/rubrics/kiro-progressive-steering.ts'
      - 'skill-forge/fixtures/eval/kiro-progressive-steering/**'

permissions:
  contents: read

# Cancel in-progress runs for the same PR — only the latest push matters.
concurrency:
  group: eval-kiro-progressive-steering-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  eval-kiro-progressive-steering:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: skill-forge
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.12"

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.bun/install/cache
            skill-forge/node_modules
          key: bun-${{ runner.os }}-${{ hashFiles('skill-forge/bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - run: bun install --frozen-lockfile

      - name: Build scenario-mixed fixture
        run: bun run dev build --harness kiro
        working-directory: skill-forge/fixtures/eval/kiro-progressive-steering/scenario-mixed

      - name: Run progressive-steering rubric
        run: |
          bun run dev eval \
            --harness kiro \
            --rubric progressive-steering \
            --build fixtures/eval/kiro-progressive-steering/scenario-mixed/expected-build \
            --json \
            --output rubric.json

      - name: Upload rubric result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: rubric-kiro-progressive-steering
          path: skill-forge/rubric.json
          retention-days: 90
