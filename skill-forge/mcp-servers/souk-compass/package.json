{
	"name": "@jhu-sheridan-libraries/souk-compass",
	"version": "0.1.0",
	"description": "Solr-backed semantic search MCP server for the context-bazaar ecosystem",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "bun test",
		"build": "bun build src/index.ts --target=node --outfile=dist/mcp-server.cjs --format=cjs",
		"lint": "biome check .",
		"lint:fix": "biome check --write ."
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.1",
		"zod": "^4.3.6",
		"@xenova/transformers": "^2.17.2"
	},
	"devDependencies": {
		"fast-check": "^4.7.0",
		"@types/bun": "^1.3.12",
		"typescript": "^6.0.3"
	},
	"license": "MIT"
}
